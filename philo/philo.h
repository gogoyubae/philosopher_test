#ifndef PHILO_H
# define PHILO_H

#include <pthread.h>
#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include <unistd.h>

int	ft_atoi(const char *str);
int ft_strncmp(const char *s1, const char *s2, size_t n);

#endif
